package main

import (
  "net/http"
)
func addRoutes(
  mux *http.ServeMux,
  // some dependencies here, eg.
  // logger *logging.Logger,
  // config Config,
  // authProxy *authProxy
) {
  // mux.Handle("/", handleHome(logger))
}
